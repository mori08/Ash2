---
name: implement-issue
description: 実装計画に基づきコードを変更し、全チェックが通るまでループする（resolve-issue Phase 2）
---

# implement-issue

プランファイルを読み込み、ブランチを作成した上で実装・検証ループを回す。
全チェック通過後にユーザーの承認を得て完了とする。

## 手順

### 1. 準備

`$ARGUMENTS` から Issue 番号を取得し、`.claude/tmp/plan-<number>.md` を読み込む。
Issue のラベルと実装方針から GIT.md の命名規則に従いブランチ名を決め、作成する。

```bash
git checkout -b <branch-name>
```

### 2. 実装ループ

a〜d を順番に実行する。**NG が出た時点で即座に e へ進み、a に戻る。**

#### a. Implement サブエージェント

Agent ツール（`subagent_type: implement`）を起動し、プランファイルのパスを渡して実装させる。
このエージェントはファイルの読み書き・編集を担う。

#### b. CI サブエージェント

Implement エージェントの出力から変更ファイル一覧を取り出し、それをプロンプトに含めて Agent ツール（`subagent_type: ci`）を起動する。
問題なければ OK を返す。問題があれば即座に NG レポートを返し、c〜d はスキップして e へ進む。

#### c. Review サブエージェント

Agent ツール（`subagent_type: review`）を起動する。
問題なければ OK を返す。問題があれば即座に NG レポートを返し、d はスキップして e へ進む。

#### d. ビジュアルチェック（ゲームを実行してテストしたい箇所がある場合）

`/run` スキルを使ってゲームを起動し、表示・挙動・操作感を確認する。

#### e. NG の処理

NG レポートの規模に応じて以下のいずれかを選択する。

**1. 軽微な修正**（typo・フォーマット・警告1件など）
ユーザーへの確認なしに修正方針を決め、プランファイルの末尾に追記して a に戻る。

**2. 中程度な修正**（ロジック変更・設計の調整など）
修正プランをユーザーに提示してレビューを依頼し、議論・承認を得てからプランファイルに追記して a に戻る。

**3. 大規模な修正**（方針の根本的な見直しが必要な場合）
このセッションを中断し、`/resolve-issue` の再実行（Phase 1 からやり直し）を提案する。

プランへの追記フォーマット（1・2の場合）：

```markdown
## 修正 <N回目>
<NG の内容と修正方針>
```

### 3. 完了報告と承認

全チェック通過後、変更の概要をユーザーに報告し、承認を得る。
