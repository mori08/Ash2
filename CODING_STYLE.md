# コーディングスタイル

フォーマットは `.clang-format` で自動管理。以下は自動化できないルールのみ記載。

## インクルード順序

1. 対応ヘッダ（`Foo.cpp` なら `Foo.hpp`）
2. `<Siv3D.hpp>`
3. その他ローカルヘッダ `"..."`
4. その他システムヘッダ `<...>`

`.clang-format` の `IncludeCategories` で自動ソートされる。

## 命名規則

（今後追加）
